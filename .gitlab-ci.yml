
# Generate sphinx rapport
create_sphinx_rapport:
  image: registry.fusiondirectory.org/fusiondirectory/fd/sphinx-cli:fd
  only:
    - branches
    - tags
  script:
    - make SPHINXOPTS='-n -W -D' html
