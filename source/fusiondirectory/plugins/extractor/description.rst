Description
===========

This plugin allows you to define a set of users, user groups, or dynamic groups to extract data into a specific format.
For now, only **CSV** is available, but additional formats will be supported in the future.

